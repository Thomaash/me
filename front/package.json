{
  "name": "front",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "test": "cypress run --component || true",
    "lint": "eslint --ext .js,.vue . || true"
  },
  "engines": {
    "node": ">=22.16"
  },
  "description": "Frontend workspace. Move your frontend sources (src/, index.html, vite.config.js, public/, tests/) here."
}
